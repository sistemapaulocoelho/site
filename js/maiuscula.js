// Função para transformar em caixa alta
// Nome
document.getElementById('nome-aluno').addEventListener('input', function () {
    this.value = this.value.toUpperCase();  // Converte para caixa alta
});

// Endereço
document.getElementById('endereco-aluno').addEventListener('input', function () {
    this.value = this.value.toUpperCase();  // Converte para caixa alta
});

// Bairro
document.getElementById('bairro-aluno').addEventListener('input', function () {
    this.value = this.value.toUpperCase();  // Converte para caixa alta
});

// Cidade
document.getElementById('cidade-aluno').addEventListener('input', function () {
    this.value = this.value.toUpperCase();  // Converte para caixa alta
}); 

// Observação
document.getElementById('observacoes').addEventListener('input', function () {
    this.value = this.value.toUpperCase();  // Converte para caixa alta
});

